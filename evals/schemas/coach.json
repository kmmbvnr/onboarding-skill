{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "message": {"type": "string"},
    "status": {"type": "string", "enum": ["continue", "done", "blocked"]},
    "active_node": {"type": "string"},
    "reason": {"type": "string"}
  },
  "required": ["message", "status", "active_node", "reason"],
  "additionalProperties": false
}
